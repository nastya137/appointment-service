# Required only for the Telegram bot. Never put this in VITE_* variables.
BOT_TOKEN=

# Used when running Python directly. Compose stores SQLite in its own volume.
DATABASE_URL=sqlite+aiosqlite:///./database.db
API_BASE_URL=http://127.0.0.1:8000
